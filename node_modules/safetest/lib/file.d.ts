import React from 'react';
import { MergedResults } from 'safetest';
type File = MergedResults['testResults'][number];
type Result = File['assertionResults'][number];
export type Test = Result & {
    id: string;
    parent: Suite;
};
export type Suite = {
    name: string;
    id: string;
    suites: Record<string, Suite>;
    tests: Record<string, Test>;
    parent?: Suite;
};
export declare const File: React.FunctionComponent<{
    file: File;
}>;
export {};
//# sourceMappingURL=file.d.ts.map