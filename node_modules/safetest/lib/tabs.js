import React from 'react';
import { ComponentsContext } from './report';
export const Tabs = ({ tabs }) => {
    const [selected, setSelected] = React.useState(0);
    const { Radio } = React.useContext(ComponentsContext);
    return (React.createElement(React.Fragment, null,
        React.createElement("div", { style: { padding: '0 8px' } },
            React.createElement(Radio, { defaultIndex: 0, options: tabs.map((t) => t.title), onChange: setSelected })),
        React.createElement("div", { style: { padding: 8 } }, tabs[selected]?.content)));
};
