import type { SvelteComponent } from 'svelte';
import { Page } from 'playwright';
import { RenderOptions } from './render';
import { Importer } from './bootstrap';
interface RenderReturn {
    /** The Playwright page object of the rendered component. */
    page: Page;
    /** Pause current test. */
    pause: () => Promise<void>;
}
type Renderable = typeof SvelteComponent;
export declare function render(elementToRender?: Renderable | ((app: Renderable) => Renderable), options?: RenderOptions): Promise<RenderReturn>;
type BootstrapArgs = Importer & {
    element: typeof SvelteComponent;
    render: (e: Renderable) => Promise<SvelteComponent>;
};
export declare const bootstrap: (args: BootstrapArgs) => Promise<SvelteComponent>;
export {};
//# sourceMappingURL=svelte.d.ts.map